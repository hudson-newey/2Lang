// These pre-processor functions fill the remaining memory address with
// zeros until the next 64, 32, 16, or 8 bit address offset

// looks at the current address space and fills to the next "n" bit factor
//
// e.g. 01 FILL_8_BIT would output 01 000000
// where the next six bits are set to zero so that the memory address is a factor of 8
#(PADDING::FILL_64_BIT) @{}
#(PADDING::FILL_32_BIT) @{}
#(PADDING::FILL_16_BIT) @{}
#(PADDING::FILL_8_BIT) @{}

// looks at the current address and pads the current memory address
// (shifting all addresses after) by n bits
#(PADDING::REVERSE_FILL_64_BIT) @{}
#(PADDING::REVERSE_FILL_32_BIT) @{}
#(PADDING::REVERSE_FILL_16_BIT) @{}
#(PADDING::REVERSE_FILL_8_BIT) @{}

// adds n number of zero bits to the current memory address regardless of the
// current position
#(PADDING::64_ZERO_BITS) 00000000 00000000 00000000 00000000 00000000 00000000 00000000 00000000
#(PADDING::32_ZERO_BITS) 00000000 00000000 00000000 00000000
#(PADDING::16_ZERO_BITS) 00000000 00000000
#(PADDING::8_ZERO_BITS) 00000000
